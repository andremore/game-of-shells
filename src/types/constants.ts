import { Mode, Speed } from "./enums";
import { Settings } from "./types";

export const defaultSettings: Settings = {
    shellNumber: 3,
    speed: Speed.NORMAL,
    chances: 1,
    mode: Mode.DEFAULT
}
