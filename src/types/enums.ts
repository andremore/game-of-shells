export enum Speed {
    SLOW = 500,
    NORMAL = 250,
    FAST = 150,
    HARDCORE = 50
}

export enum Mode {
    DEFAULT = 'default',
    ROGUELIKE = 'roguelike'
}

export enum ContainerIds {
    GAME = 'game-container',
    SHELL = 'shell-container'
}
