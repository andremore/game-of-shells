export enum Speed {
    SLOW = 'slow',
    NORMAL = 'normal',
    FAST = 'fast',
    HARDCORE = 'hardcore'
}

export enum Mode {
    DEFAULT = 'default',
    ROGUELIKE = 'roguelike'
}

export enum ContainerIds {
    GAME = 'game-container',
    SHELL = 'shell-container'
}
